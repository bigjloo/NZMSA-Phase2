import { makeStyles, createStyles} from "@material-ui/core/styles"

const ShareDialogStyles = makeStyles(() => createStyles({
    dialogContentText: {
        margin: "1rem 0"
    }
}))

export default ShareDialogStyles