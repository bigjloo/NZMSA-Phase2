import { makeStyles, createStyles } from "@material-ui/core/styles"

const SharedContentStyles = makeStyles(() => createStyles({
    header : {
        margin: "1rem 2rem"
    }
}))

export default SharedContentStyles
